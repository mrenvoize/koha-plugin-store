{
  # Github user access token
  github_user_access_token => "YOUR_TOKEN_HERE",
  # PostgreSQL DSN - NOTE: Use single quotes to avoid Perl interpreting @ as array interpolation
  pg_dsn => 'postgresql://user:password@localhost:5432/database',
};